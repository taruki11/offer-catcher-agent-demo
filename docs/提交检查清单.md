# 提交检查清单

## 1. Demo 链接

- 3060 远程：`http://<3060_IP>:8502`
- 本地启动：`streamlit run app.py`

## 2. 方案说明文件

- [x] `docs/方案说明_提交版.md`（960 字）
- [x] 包含：问题诊断、方案设计、AI 选型、Eval、迭代记录、效果评估

## 3. 演示视频

- [x] 分镜脚本：`docs/演示视频分镜.md`（3 分钟）

## 4. 运行命令

```bash
# 本地 Demo
pip install streamlit
streamlit run app.py

# 本地 Eval
python scripts/run_eval.py

# 3060 同步
scp -r app.py data src eval scripts reports docs 3060:D:/Pycharm_workplace/offer_catcher_agent_demo_20260602/

# 3060 Demo
cd D:\Pycharm_workplace\offer_catcher_agent_demo_20260602
streamlit run app.py --server.address 127.0.0.1 --server.port 8502 --server.headless true

# 3060 Eval
cd D:\Pycharm_workplace\offer_catcher_agent_demo_20260602
python scripts\run_eval.py
```

## 5. 3060 验证结果

| 检查项 | 结果 |
|--------|------|
| py_compile | OK |
| core eval (8/8) | PASS |
| stress eval (7/7) | PASS |
| test_evidence.py (6/6) | PASS |
| test_data_ingestion.py | PASS |
| analyze_job_corpus.py | PASS |
| eval_corpus_quality.py | PASS |
| check_deploy_ready.py (13/13) | PASS |
| Streamlit health (/_stcore/health) | 200 ok |

### Eval 指标

| 指标 | 值 |
|------|-----|
| Core Eval | **8/8 PASS (100%)** |
| Stress Eval | **7/7 PASS (100%)** |
| Match Top1 Acc | **100%** |
| Priority Top1 Acc | **85.7%** |
| Action Acc | **87.5%** |
| Recall@5 | **100%** |
| Corpus Eval Top1 方向命中 | **>= 71%** |
| Corpus Eval 空字段 | **0** |

残留：E5_PRIORITY×1（NLP→LLM 方向迁移）、E9_ACTION×2（城市/毕业外部差异）

## 6. 已知边界

1. **岗位库 500 条**：含 452 条 Hugging Face 公开数据 + 48 条 curated seed，非真实企业 API
2. **8 个 golden cases**：样本量不足以做全面评估，建议扩到 15-20
3. **无 LLM API 模式 Eval**：当前仅测试规则版，LLM 增强后效果待验证
4. **无真实投递数据**：PassScore 和 RiskScore 依赖规则，需真实反馈校准
5. **纯关键词召回**：尚未接入 embedding 语义召回（计划下一步）
6. **curated seed 边界**：已在 UI 和文档中明确标注，不冒充真实数据

## 7. 下一步建议

1. 扩充 golden cases 至 15-20 个，覆盖更多边界情况
2. 接入 LLM API 并对比规则版/LLM 版 Eval 指标
3. 增加 embedding 语义召回（bge-small-zh）
4. 公网部署（CloudStudio / ngrok）
5. 接入 Greenhouse/Lever/Ashby 公共 ATS API 扩大真实数据
